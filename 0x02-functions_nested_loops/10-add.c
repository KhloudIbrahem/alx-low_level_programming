#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @num1: the number 1.
 * @num2: the number 2.
 * Return: the sum of them.
 */
int add(int num1, int num2)
{
	int res = 0;

	res = num1 + num2;
	return (res);
}
